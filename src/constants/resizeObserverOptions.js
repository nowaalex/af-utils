/* It is important, because all observed dimensions ( td widths, tr heights ) must include paddings and borders */
const resizeObserverOptions = {
    box: "border-box"
};

export default resizeObserverOptions;