const metadata = {
    description: "All dimensions are calculated on the fly. Items are added to ResizeObserver when mounted. \
    Use estimateditemsize to improve scrolling experience."
} as const;

export default metadata;
