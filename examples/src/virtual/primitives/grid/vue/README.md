Dynamic two-dimensional virtualization uses one Vue model for rows and one for
columns. Row heights come from the first rendered column and column widths from
the first rendered row; masonry layout is not supported.
