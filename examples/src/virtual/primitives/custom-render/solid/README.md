Native table layout cannot place the normal positioning wrapper between
`tbody` and `tr`, so this Solid example uses measured rows and spacer rows
while preserving the browser's adaptive table layout.
