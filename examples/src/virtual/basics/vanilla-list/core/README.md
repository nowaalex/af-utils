This standalone example shows the framework-neutral `VirtualScroller` and
`VirtualScrollerLayout` APIs with ordinary DOM nodes. It demonstrates range
rendering, item measurement, offset/index conversion, programmatic scrolling,
and measurement invalidation without a framework adapter.
