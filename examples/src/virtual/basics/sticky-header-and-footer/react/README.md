This example keeps a header and footer inside the scrolling viewport while the
virtual item range remains measured and independently positioned.

The sticky elements sit beside the total-size spacer and reserve usable
viewport space without becoming virtual items.
