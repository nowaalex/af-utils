This is the smallest complete virtual list for each framework. Application data
stays in the framework while the adapter renders and measures only the current
visible range.

Start here for an ordinary list. The code includes the required viewport,
layout binding, stable item rendering, and cleanup in framework-native syntax.
