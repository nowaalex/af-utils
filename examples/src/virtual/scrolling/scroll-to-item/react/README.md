This example scrolls to an item by index while row sizes and the collection
length change. It covers alignment, smooth scrolling, sticky controls, and
measurement correction through the public scrolling API.
