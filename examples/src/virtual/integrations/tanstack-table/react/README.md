This example combines TanStack Table v9 sorting and filtering row models with
AF Virtual Scroll. TanStack owns the headless table state; the virtual scroller
renders and measures only the visible rows while preserving stable row identity.
