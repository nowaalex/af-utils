---
import type { CollectionEntry } from "astro:content";

const { slug } = Astro.props;

interface Props {
    slug: CollectionEntry<"reference">["id"];
}
---

<a href={`/virtual/reference/${slug}`}><slot /></a>
