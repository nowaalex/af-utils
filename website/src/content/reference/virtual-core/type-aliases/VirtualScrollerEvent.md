---
title: "Type Alias: VirtualScrollerEvent"
description: VirtualScrollerEvent is exported as a constant, so a separate type is needed to emulate enum behavior.
package: "@af-utils/virtual-core"
symbol: VirtualScrollerEvent
kind: typealias
referencePath: /virtual/reference/virtual-core/type-aliases/VirtualScrollerEvent
generated: true
---

# VirtualScrollerEvent

```ts
type VirtualScrollerEvent = typeof RANGE | typeof SCROLL_SIZE | typeof SIZES;
```

`VirtualScrollerEvent` is exported as a constant, so a separate type is
needed to emulate enum behavior.
