---
title: "Variable: virtualContainer"
description: Attach an arbitrary element as the model's rendered-item container.
package: "@af-utils/virtual-svelte"
symbol: virtualContainer
kind: variable
referencePath: /virtual/reference/virtual-svelte/variables/virtualContainer
generated: true
---

# virtualContainer

```ts
const virtualContainer: Action<HTMLElement, VirtualScroller>;
```

Attach an arbitrary element as the model's rendered-item container.
