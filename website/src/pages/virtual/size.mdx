---
layout: layouts/virtual/VirtualDocumentation.astro
title: "Bundle size"
description: "See the combined production payload of virtual core and each framework adapter, with raw, minified, gzip, and Brotli measurements."
---

import SizeInfo from "components/SizeInfo.astro";
import sizes from "bundleSizes";
import { addBundleSizes } from "utils/bundleSizes";
import { virtualAdapterPackages } from "utils/frameworks";

export const coreSize =
    sizes["@af-utils/virtual-core"]["./dist/index.production.js"];

# Virtual bundle size impact

An application using a framework adapter also includes virtual core. The table
therefore shows the combined payload users evaluate, rather than presenting the
smaller adapter file by itself.

<SizeInfo
    items={[
        [coreSize, "Core only"],
        ...virtualAdapterPackages.map(name => [
            addBundleSizes(coreSize, sizes[name]["./dist/index.js"]),
            `Core + ${name.replace("@af-utils/virtual-", "")}`
        ])
    ]}
/>

## How the numbers are produced

The website build first builds the current workspace packages, reads their
published ESM exports, minifies with SWC, and generates raw, gzip, and Brotli
sizes. Peer frameworks such as React, Vue, or Lit are external and are not
included.

Core and adapter files are measured separately and added in the table. This is
a conservative compressed estimate: a final application bundle may compress
shared tokens together and tree-shake unused exports. Use these values to compare
adapter impact, then measure the production application for an exact result.
