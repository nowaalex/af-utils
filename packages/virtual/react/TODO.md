# TODO

## Atomic range snapshot

- Expose an atomic cached range snapshot containing `revision`, `from`, and
  `to`; render children from that snapshot instead of separately reading
  mutable model fields.
