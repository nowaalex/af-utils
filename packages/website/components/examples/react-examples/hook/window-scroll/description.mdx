import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

{""}<AutoLink href={docHref("virtual-react", "useScroller")}>useScroller hook</AutoLink> allows to set any element or window
as scroller. It gives you more flexibility than ref. You can even use elements from outside of current component.
