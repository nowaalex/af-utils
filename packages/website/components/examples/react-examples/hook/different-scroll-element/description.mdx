import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

You can use <AutoLink href={docHref("virtual-core", "VirtualScroller.setContainer")}>VirtualScroller.setContainer</AutoLink> in case something is rendered
before primary virtual scroller items. It may be also useful in [window scroll](./window-scroll) scenario.
