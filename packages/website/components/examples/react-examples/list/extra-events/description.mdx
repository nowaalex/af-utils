import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

Virtual scroll model allows multiple <AutoLink href={docHref("virtual-react", "Subscription")}>subscriptions</AutoLink>. This helps to optimize performance, as only needed components are rerendered.
