import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

{""}<AutoLink href={docHref("virtual-react", "Subscription")}>Subscription</AutoLink> component can be used to subscribe
to <AutoLink href={docHref("virtual-core", "VirtualScrollerEvent")}>VirtualScroller events</AutoLink>. This helps to optimize performance, as only needed components are rerendered.
