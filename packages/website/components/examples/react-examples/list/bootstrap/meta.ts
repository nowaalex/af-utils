import { Metadata } from "next";

const metadata = {
    description:
        "Virtual List supports component prop, so it can be easily integrated with Bootstrap or Material UI."
} satisfies Metadata;

export default metadata;
