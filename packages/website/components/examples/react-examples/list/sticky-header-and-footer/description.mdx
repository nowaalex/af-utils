import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

{""}<AutoLink href={docHref("virtual-react", "List")}>List</AutoLink> header and footer
can have sticky positioning.
Connect them with
{""}<AutoLink href={docHref("virtual-core", "VirtualScroller.setStickyHeader")}>setStickyHeader</AutoLink>
or <AutoLink href={docHref("virtual-core", "VirtualScroller.setStickyFooter")}>setStickyFooter</AutoLink> and add `position: sticky` style.
