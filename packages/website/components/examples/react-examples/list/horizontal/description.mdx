import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

{""}<AutoLink href={docHref("virtual-react", "useVirtual")}>useVirtual</AutoLink> hook
supports <AutoLink href={docHref("virtual-core", "VirtualScrollerInitialParams.horizontal")}>horizontal</AutoLink> param
for virtualizing columns instead of rows.
All necessary styles are applied to <AutoLink href={docHref("virtual-react", "List")}>List</AutoLink> component automatically.
