import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

{""}<AutoLink href={docHref("virtual-react", "List")}>List</AutoLink> supports `component`
prop, so it can be easily integrated with Material UI or <AutoLink href="./bootstrap">Bootstrap</AutoLink>. Also `disablePadding` should
be passed to `MuiList`, because scroll element offsets are not supported.
