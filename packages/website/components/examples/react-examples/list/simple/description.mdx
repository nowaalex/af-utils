import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

Virtual scroll can be used to render huge lists without performance problems.
All dimensions are calculated automatically.
Just provide <AutoLink href={docHref("virtual-core", "VirtualScrollerRuntimeParams.itemCount")}>itemCount</AutoLink>
and notify model about new elements with the help of <AutoLink href={docHref("virtual-core", "VirtualScroller.el")}>VirtualScroller.el</AutoLink>.
