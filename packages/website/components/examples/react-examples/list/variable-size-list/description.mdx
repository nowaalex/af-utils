import docHref from "utils/docHref";
import AutoLink from "components/AutoLink";

All dimensions are calculated on the fly. Items are added to `ResizeObserver` when mounted.
Use <AutoLink href={docHref("virtual-core", "virtualscrollerruntimeparams.estimateditemsize")}>estimatedItemSize</AutoLink> to improve scrolling experience.
