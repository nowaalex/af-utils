import { Metadata } from "next";

const metadata = {
    description:
        "All dimensions are calculated on the fly. Items are added to ResizeObserver when mounted. \
Use estimateditemsize to improve scrolling experience."
} satisfies Metadata;

export default metadata;
