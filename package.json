{
  "private": true,
  "author": "Alex Fomin <nowaalex@gmail.com> (https://github.com/nowaalex/)",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn wsrun start",
    "prepublish": "yarn wsrun -x website --stages build",
    "release": "yarn prepublish && yarn changeset publish",
    "test": "jest"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "browserslist": [
    ">0.5%",
    "not dead",
    "not ie <= 11",
    "not op_mini all",
    "not ios_saf < 13"
  ],
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.2",
    "@changesets/cli": "^2.20.0",
    "wsrun": "^5.2.4"
  }
}
