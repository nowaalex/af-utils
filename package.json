{
  "private": true,
  "author": "Alex Fomin <nowaalex@gmail.com> (https://github.com/nowaalex/)",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn wsrun --collect-logs start",
    "build": "yarn wsrun --stages build && cp -r packages/website/docs .",
    "test": "jest",
    "clear-test-cache": "jest --clearCache"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "browserslist": [
    ">0.5%",
    "not dead",
    "not ie <= 11",
    "not op_mini all",
    "not ios_saf < 13"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
